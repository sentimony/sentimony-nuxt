# 2026-08-10 — baseline першого завантаження перед міграцією на Cloudflare

Знімок швидкості кожного типу сторінки й кожного класу асета до будь-якої зміни
інфраструктури. Числа зняті одним інструментом (`npm run perf:baseline`), щоб
заміри «до» і «після» можна було порівнювати напряму. Колонки
`cloudflare-dns` і `cloudflare-workers` заповнюються у фазах 1 і 3
[плану міграції](../superpowers/plans/2026-07-26-cloudflare-migration.md).

Файл названий за датою прогону (2026-08-10), а не за датою плану (2026-07-26).

## Контекст замірів

| параметр | значення |
|---|---|
| дата/час прода | 2026-08-10 13:00 UTC (16:00 EEST, `+0300`) |
| дата/час stage | 2026-08-10 13:04 UTC |
| географія клієнта | Київ, Україна (AS12963) |
| з'єднання | домашній провідний доступ, без VPN |
| Node | v24.15.0 |
| прогонів на ціль | 5 (`PERF_RUNS=5`) |
| catalog source прода | `supabase` — перевірено емпірично: `/api/tracks` віддає 735 із 744 треків з `audio_url`, чого у firebase-режимі не буває |
| DNS на момент замірів | `sentimony.com` A → `104.198.14.52`, `www` CNAME → `sentimony-nuxt.netlify.com`, делегування на 12 серверів `srv53` |
| артефакти | [`data/netlify-prod.json`](data/netlify-prod.json), [`data/netlify-stage.json`](data/netlify-stage.json) |

`netlify env:list --context production` під поточним CLI-акаунтом не повернув
жодної змінної (акаунт має read-only права), тому catalog source визначено
через відповідь API, а не через конфігурацію.

## Метод

На кожну ціль виконується 5 пар вимірювань:

- **cold** — URL із унікальним параметром `_pb=<uuid>` і `cache: no-store`, тобто
  гарантований промах повз усі кеші;
- **warm** — чистий URL двічі поспіль, зараховується друга відповідь.

`ttfbMs` — час до резолву `fetch`, тобто до заголовків. `p95` рахується
nearest-rank, без інтерполяції, щоб число було відтворюваним. Медіана береться
по 5 прогонах.

## Головна знахідка: HTML на Netlify не кешується

Кожна сторінка прода повертає
`Cache-Status: "Netlify Durable"; fwd=bypass, "Netlify Edge"; fwd=miss`.
Жодного `hit` на жодній сторінці за всі 100 прогонів. Наслідок видно у числах:
warm-медіана по сторінках (434 ms) практично дорівнює cold-медіані (455 ms) —
повторний запит не дешевший за перший, кожен hit доходить до SSR-функції.

Це узгоджується з тим, що page-level ISR прибрано з `routeRules` під час
cache hot-fix, а `Netlify-CDN-Cache-Control` виставляється тільки на
`server/api/**`, не на сторінки.

Асети поводяться протилежно: `content.sentimony.com` віддає
`"Netlify Edge"; hit; ttl≈31 5xx xxx`, тобто картинки кешуються на рік.

Практичний висновок для міграції: **поточна планка 400-600 ms — це не вартість
CDN, а вартість SSR на кожен запит.** Якщо на Workers увімкнути кешування
сторінок, виграш буде більший за різницю між платформами; якщо не вмикати,
порівнюватиметься лише швидкість runtime.

## Сторінки

| ціль | prod cold med | prod cold p95 | prod warm med | prod bytes | stage cold med | stage warm med | cloudflare-dns | cloudflare-workers |
|---|---|---|---|---|---|---|---|---|
| `/` | 447 | 603 | 494 | 269 758 | 284 | 315 | | |
| `/releases` | 456 | 580 | 430 | 329 416 | 452 | 436 | | |
| `/releases/psytrance` | 515 | 653 | 435 | 290 115 | 292 | 286 | | |
| `/releases/psychill` | 599 | 633 | 391 | 280 040 | 288 | 289 | | |
| `/artists` | 445 | 492 | 420 | 305 958 | 276 | 357 | | |
| `/artists/all` | 607 | 1212 | 567 | 798 931 | 407 | 297 | | |
| `/videos` | 451 | 617 | 380 | 251 700 | 299 | 214 | | |
| `/events` | 402 | 429 | 392 | 250 299 | 292 | 381 | | |
| `/playlists` | 498 | 574 | 411 | 249 331 | 237 | 272 | | |
| `/friends` | 491 | 550 | 459 | 245 589 | 234 | 217 | | |
| `/tracks` | 927 | 1313 | 984 | 798 391 | 1110 | 1082 | | |
| `/news` | 580 | 618 | 532 | 338 174 | 470 | 345 | | |
| `/contacts` | 381 | 402 | 366 | 245 083 | 246 | 246 | | |
| `/signin` | 399 | 690 | 385 | 248 499 | 278 | 231 | | |
| `/release/vorg-cyber-soul-chill` | 454 | 1201 | 502 | 267 151 | 396 | 322 | | |
| `/artist/irukanji` | 720 | 1667 | 507 | 313 744 | 511 | 346 | | |
| `/video/irukanji-live-eve8-2022` | 429 | 763 | 435 | 260 797 | 353 | 277 | | |
| `/event/five-years` | 433 | 788 | 385 | 268 243 | 239 | 270 | | |
| `/playlist/goa-trance` | 590 | 687 | 560 | 496 518 | 455 | 253 | | |
| `/friend/treetrolla` | 348 | 812 | 433 | 245 709 | 275 | 281 | | |

Медіана по всіх сторінках прода: **cold 455 ms, warm 434 ms**.

`/tracks` — найдорожча сторінка на обох хостах (927 ms cold на проді, 1110 ms на
stage) і найважча разом із `/artists/all` (≈798 KB). Це два кандидати на окрему
оптимізацію незалежно від платформи.

Найбільші p95-викиди — `/artist/irukanji` (1667 ms), `/tracks` (1313 ms),
`/artists/all` (1212 ms), `/release/…` (1201 ms). Розрив між медіаною і p95
у 2-4 рази — це холодні старти serverless-функції.

### Чому stage не можна порівнювати з продом

Stage віддає помітно менше байтів на тих самих маршрутах (`/` — 152 KB проти
270 KB, `/videos` — 96 KB проти 252 KB). Це різниця збірок, а не платформи:
stage розгорнутий з іншого стану коду. Тому stage-колонки служать лише як
власна точка відліку для stage, а висновок про міграцію робиться порівнянням
прода з продом.

## Асети

| ціль | prod cold med | prod cold p95 | prod warm med | bytes | stage cold med | stage warm med | cloudflare-dns | cloudflare-workers |
|---|---|---|---|---|---|---|---|---|
| `image thumb` (`content.sentimony.com`, `_th`) | 44 | 204 | 97 | 19 537 | 46 | 44 | | |
| `image full` (`content.sentimony.com`, `_xl`) | 129 | 271 | 266 | 314 823 | 47 | 99 | | |
| `audio track` (`pub-*.r2.dev`) | 111 | 207 | 127 | 16 960 111 | 119 | 84 | | |

Картинки вже на Netlify Edge з річним TTL, тож від перемикання DNS на Cloudflare
(фаза 1, режим DNS-only) вони не мають змінитись зовсім. `_xl` важить 315 KB —
це найважчий елемент сторінки релізу, і саме він визначає LCP.

Аудіо з `r2.dev` не віддає розпізнаваного cache-заголовка (`unknown`); один трек
важить 17 MB. Це аргумент за Task 17 плану (`audio.sentimony.com`), але рішення
приймається за виміряною різницею, а не наперед.

## Розмір майбутнього Worker-бандла

Збірка `NITRO_PRESET=cloudflare_module npm run build` пройшла успішно
(`nitro.preset` уже читає `NITRO_PRESET`).

| метрика | значення |
|---|---|
| `.output/server` | 4.3 MB |
| `.output/public` | 6.6 MB |
| сумарно за звітом Nitro | 3.44 MB raw / 923 KB gzip (разом із `.map`) |
| код без source maps | 2.85 MB raw / **0.67 MiB gzip** (177 `.mjs`) |

Поріг, заданий планом (gzip > 1 MiB — блокер), **не перевищено**: 0.67 MiB.
Число отримане gzip-стисненням конкатенації всіх `.mjs`, тому це наближення —
Wrangler рахує модулі окремо. Точну цифру дасть перший `wrangler deploy`
у Task 12.

## Що з цього випливає

1. Головний резерв швидкості — **кешування HTML**, а не зміна платформи.
   Поточні 400-600 ms — це SSR на кожен запит.
2. `/tracks` і `/artists/all` (≈798 KB, до 1.1 s) варті окремої роботи
   незалежно від того, де вони хостяться.
3. Розмір бандла міграцію не блокує.
4. Асети чіпати у фазі 1 не потрібно: вони вже кешуються.

## Обмеження цих замірів

- Одна географічна точка (Київ) і одне з'єднання. Заміри «після» треба знімати
  з тієї самої машини, інакше порівняння недійсне.
- PageSpeed Insights пропущено: `PSI_API_KEY` не заданий. Лабораторні метрики
  (LCP, TBT, CLS) у baseline відсутні; є лише мережеві.
- 5 прогонів дають надійну медіану, але p95 по 5 значеннях — це фактично
  максимум, а не справжній 95-й перцентиль.
